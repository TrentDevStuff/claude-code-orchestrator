#!/bin/bash
# Claude Code Orchestrator - Mac Installer
# Double-click this file in Finder to install dependencies and set up directories
cd "$(dirname "$0")/.."
exec bash install-claude-orchestrator.sh
