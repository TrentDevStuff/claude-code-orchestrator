#!/bin/bash
# Claude Code Orchestrator - macOS Prerequisites Installer
# Double-click this file in Finder to install Homebrew, Python, Claude Code, and watchdog
cd "$(dirname "$0")/.."
exec bash install-macos-prereqs.sh
